import React from 'react';

const Dashboard: React.FC = () => {
    return (
        <div>
            <h1>ECU Tuning Dashboard</h1>
            <p>Welcome to the ECU tuning dashboard!</p>
            {/* Add chart components here */}
            {/* Add tuning parameters here */}
        </div>
    );
};

export default Dashboard;